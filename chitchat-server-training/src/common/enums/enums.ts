export enum MessageType {
  TEXT = 'text',
  FILE = 'file',
  AUDIO = 'audio',
  LINK = 'link',
  VIDEO = 'video',
  STICKERS = 'stickers',
}

export enum UserStatus {
  ONLINE = 'online',
  OFFLINE = 'offline',
}

export enum ChannelStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}

export enum ChatroomType {
  DIRECT = 'direct',
  GROUP = 'group',
}

export enum RecentlyMessageStatus {
  SEEN = 'seen',
  NOTSEEN = 'notseen',
}
