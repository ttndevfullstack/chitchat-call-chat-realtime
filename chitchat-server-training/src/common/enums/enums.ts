export enum MessageType {
  Text = 'text',
  File = 'file',
  Audio = 'audio',
  Link = 'link',
  Video = 'video',
  Stickers = 'stickers',
}

export enum UserStatus {
  Online = 'online',
  Offline = 'offline',
}
